import java.util.Scanner

fun main() = with(Scanner(System.`in`)) {
    var a = nextInt()
    var b = nextInt()
    
    if (a > b) {
        println(">")
    }
    else if (a < b) {
        println("<") 
    }
    else {
        println("==")
    }
}